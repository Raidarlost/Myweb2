import Shell from "@/components/Shell";
import ContactContent from "@/components/ContactContent";

export const metadata = {
  title: "Contact — Raidar Lost",
  description: "Get in touch with Raidar Lost for custom beats, collaborations, and licensing inquiries.",
};

export default function ContactPage() {
  return (
    <Shell>
      <ContactContent />
    </Shell>
  );
}
